# Haskell / Stack build artifacts
.stack-work/
dist/
*.cabal
registry-server-bin

# Native libraries (libjinja, downloaded per platform, see .github/actions/haskell-setup)
lib/

# Local config with secrets - committed templates live next to them as *.example
config/registry/application.yml
config/registry/application-test.yml
config/registry/build-info.yml
config/registry/integration.yml
config/registry/integration-test.yml

# macOS
.DS_Store
